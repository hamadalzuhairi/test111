/**
 * Edit these two values for your event — nothing else in the app needs to
 * change. CAMPAIGN_NAME appears in the README/meta only (not on-screen,
 * per the design brief's booth-message-first copy); QR_URL drives the
 * closing screen's QR code.
 */
export const CAMPAIGN_NAME = "KSAU-HS Innovation & Health Awareness Campaign";
export const QR_URL = "https://example.org/ai-in-radiology";
